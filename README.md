# PrepKit

A tool to streamline competitive programming and machine learning workflows.

## System Dependencies

This tool uses `libclang` to parse C++ code. You will need to have the `libclang` library installed on your system. 

**For Debian/Ubuntu:**

```bash
sudo apt-get update && sudo apt-get install -y libclang-16
```
